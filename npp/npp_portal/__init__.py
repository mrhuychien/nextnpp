# -*- coding: utf-8 -*-
# Python package for the "NPP Portal" module declared in modules.txt.
# Required by frappe.model.sync.sync_for() — it resolves every module via
# frappe.get_module("npp.npp_portal"), so this package must be importable
# even though the module ships no standard DocTypes (only Custom Fields).
